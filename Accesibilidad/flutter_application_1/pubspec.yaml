name: ayuda_auditiva
description: "Formulario accesible con voz para Flutter Web."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🗣️ Texto a voz (funciona en Web, Android y iOS)
  flutter_tts: ^3.8.3

  # 🎤 Reconocimiento de voz multiplataforma
  speech_to_text: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true